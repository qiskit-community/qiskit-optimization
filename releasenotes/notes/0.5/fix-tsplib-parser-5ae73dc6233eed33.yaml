---
fixes:
  - |
    Fixed an issue that :meth:`~qiskit_optimization.application.tsp.Tsp.parse_tsplib_format` did not parse TSPLIB files
    correctly in all cases; in particular if extra whitespace existed around keywords or if an EOF keyword was present.
